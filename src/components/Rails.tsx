/**
 * THE ground: the exposed 12-column hairline grid, painted behind the content.
 * The grid is content, not scaffolding to delete.
 */
export const Rails = () => <div className="rails" aria-hidden="true" />;
